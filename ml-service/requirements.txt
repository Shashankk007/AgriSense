# ===================================================================
# AgriSense ML Service — Python Dependencies
# ===================================================================
# Generated by analyzing every .py file in the project.
# Install with:  pip install -r requirements.txt
# ===================================================================
--extra-index-url https://download.pytorch.org/whl/cpu


# ---- Web Framework & Server ----
fastapi==0.114.1
uvicorn[standard]==0.30.6
python-multipart==0.0.32          # For file upload support in FastAPI
requests==2.34.2                  # HTTP client (cli_chat.py, check_models.py, test.py)

# ---- Database (MongoDB) ----
pymongo==4.8.0                    # MongoDB driver (used everywhere)

# ---- Google Earth Engine (NDVI Service) ----
earthengine-api==1.7.31           # GEE SDK (gee/gee_init.py, services/ndvi_service.py)

# ---- LangChain Core ----
langchain==1.3.10
langchain-core==1.4.8             # Base types: messages, prompts, output parsers, runnables
langchain-community==0.4.2        # Community integrations (FastEmbedEmbeddings)
langchain-text-splitters==1.1.2   # RecursiveCharacterTextSplitter (loaders/document_loader.py)

# ---- LangChain LLM Providers ----
langchain-google-genai==4.2.5     # ChatGoogleGenerativeAI (chains/chat_chain.py, agents/memory_extractor.py)
langchain-groq==1.1.3             # ChatGroq fallback LLM (chains/chat_chain.py)

# ---- LangChain MongoDB Integration ----
langchain-mongodb==0.8.0          # MongoDBAtlasVectorSearch, MongoDBChatMessageHistory

# ---- Embeddings (Local — replaces broken Google Embeddings API) ----
fastembed==0.8.0                  # FastEmbedEmbeddings (memory/long_term.py, vectorstore/mongo_vector.py)

# ---- Pydantic & Settings ----
pydantic==2.13.4                  # Data validation (models/schemas.py)
pydantic-settings==2.14.2         # Env-based config (config/settings.py)

# ---- Environment ----
python-dotenv==1.0.1              # .env file loading (app.py)

# ---- Google API (transitive but pinned for stability) ----
google-generativeai==0.8.6
google-genai==2.9.0
google-api-python-client==2.197.0
google-auth==2.55.0

# ---- Deep Learning & Computer Vision ----
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
